# Database Configuration
# PostgreSQL (recommended for production)
DATABASE_URL=postgresql://saferide_user:yourpassword@localhost:5432/saferide_db

# SQLite (for development without Docker - comment out DATABASE_URL above)
# DATABASE_URL=sqlite:///ride.db

# Testing (optional - separate test database)
# TEST_DATABASE_URL=postgresql://saferide_user:yourpassword@localhost:5432/saferide_test

# JWT Secret Key
# Generate a secure random secret for production:
# python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-change-this-in-production
